# Repository Naming

Kungfu paper repositories use the `paper-*` prefix.

The prefix describes the role of the repository:

```text
paper-kungfu-product-white-paper
```

The repository is a publication artifact producer. A future papers website may
consume its Buildchain publication manifest and decide layout, navigation, and
visual presentation. The paper repository should not become the site renderer.
